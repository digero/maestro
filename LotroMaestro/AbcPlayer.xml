<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project default="AbcPlayerExe" name="Create Runnable Jar for Project LotroMaestro">
    <property name="output" value="${basedir}/output" />
	<property name="build" value="${basedir}/build" />
	<property name="devenv" value="C:/Program Files (x86)/Microsoft Visual Studio 10.0/Common7/IDE/devenv.exe" />
    <target name="AbcPlayerExe">
        <jar destfile="${output}/AbcPlayer.jar" basedir="${basedir}/bin" excludes="com/digero/maestro/view/**,com/digero/maestro/MaestroMain.class">
            <manifest>
                <attribute name="Main-Class" value="com.digero.abcplayer.AbcPlayer"/>
                <attribute name="Class-Path" value="."/>
            </manifest>
        </jar>
    	<copy file="${basedir}/LotroInstruments.sf2" todir="${output}"/>
    	<copy file="${basedir}/TableLayout.jar" todir="${output}"/>
    	<copy file="${basedir}/gervill.jar" todir="${output}"/>
    	<copy file="${build}/abcplayer.ico" todir="${output}"/>
    	<copy file="${build}/WinRun4J.exe" tofile="${output}/AbcPlayer.exe" overwrite="true"/>
    	<exec dir="${build}" executable="${build}/rcedit.exe" failonerror="true">
    		<arg value="/W"/>
    		<arg value="${output}/AbcPlayer.exe"/>
    		<arg value="${build}/AbcPlayer.rcedit.ini"/>
    	</exec>
    </target>
	<target name="AbcPlayerInstaller" depends="AbcPlayerExe">
		<exec dir="${build}" executable="${devenv}" failonerror="true">
			<arg value="/build"/>
			<arg value="Release"/>
			<arg value="AbcPlayerInstaller.sln"/>
		</exec>
	</target>
</project>
